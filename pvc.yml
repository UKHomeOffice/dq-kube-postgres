kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jira-backup
  annotations:
    volume.beta.kubernetes.io/storage-class: gp2-encrypted
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
